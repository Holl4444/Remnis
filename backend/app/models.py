# Pydantic models - like TypeScript interfaces but with validation
# Validate incoming json data and auto generates docs
# Use separate file when there are lots of models across multiple routers

